apiVersion: 1

providers:
  - name: TBA Dashboards
    orgId: 1
    folder: TBA
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
